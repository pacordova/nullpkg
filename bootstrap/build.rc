#!/bin/rc

cd /usr/nullpkg && make
. /usr/nullpkg/bootstrap/functions.rc
. /etc/make.conf

cd cache

ldconfig && /usr/nullpkg/misc/locale.rc

tar -xhf sources.tar.xz
mv sources/* .

__emerge python python-setuptools python-pip;
sleep 3 &&
python3 -m pip install meson &&
python3 -m pip install docutils

__emerge $LIB
__emerge $BASE
__emerge $SYS
